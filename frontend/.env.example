# URL base da API. Em desenvolvimento, o proxy do Vite encaminha /api/v1
# para o backend em http://localhost:8000, então normalmente não é preciso definir.
# Em produção, aponte para a URL pública da API, por exemplo:
# VITE_API_URL=https://api.seudominio.com.br/api/v1
VITE_API_URL=/api/v1
